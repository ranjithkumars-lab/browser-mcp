# Architecture Diagrams

Place architecture and sequence diagrams here. Recommended formats:

- Mermaid (rendered by MkDocs Material) in `.mmd` files
- Exported images (`.png`, `.svg`)

See `docs/Architecture.md` for an ASCII layer diagram of the system.
