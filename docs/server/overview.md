# MCP Server Integration
## Overview
The MCP Server (`browser_mcp.server`) exposes the Browser Core to AI agents via the Model Context Protocol. It bridges tools, resources, and notifications to an abstract protocol transport layer.
