# Browser Events Engine
## Overview
The Browser Events Engine (`browser_mcp.events`) formalizes the `EventBus` into a domain-aware event infrastructure. It allows plugins, managers, and external clients to subscribe to structured system events.
