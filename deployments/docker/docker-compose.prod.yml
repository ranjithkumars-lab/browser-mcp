services:
  enterprise-mcp:
    image: ghcr.io/your-org/enterprise-mcp-server:latest
    environment:
      ENTERPRISE_MCP_ENV: production
      ENTERPRISE_MCP_SERVER__SECURITY__ENABLED: "true"
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config:ro
    restart: unless-stopped
