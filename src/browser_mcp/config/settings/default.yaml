browser:
  engine: chromium
  headless: true
  slow_mo: 0
  viewport:
    width: 1280
    height: 720
  locale: null
  timezone: null
  downloads_dir: null
  user_agent: null
  ignore_https_errors: false

pool:
  max_browsers: 10
  max_contexts_per_browser: 10
  max_pages_per_context: 50

profiles:
  directory: "~/.browser-mcp/profiles"
  default_profile: temporary
  default_persistent: false

navigation:
  allowed_domains: []
  blocked_domains: []
  allow_redirects: true
  max_redirects: 10
  allowed_schemes: ["http", "https", "file"]
  default_strategy: normal
  blocked_extensions: []
  allowed_ports: []
  max_navigation_depth: null

timeouts:
  default_timeout_ms: 30000
  navigation_timeout_ms: 30000
  interaction_timeout_ms: 10000
  wait_timeout_ms: 10000

transfer:
  download_directory: "~/.browser-mcp/artifacts"
  max_file_size_bytes: 524288000
  allowed_extensions: []
  allowed_mime_types: []
  checksum_algorithm: sha256
  collision_strategy: auto_rename
  cleanup_policy: on_failure
